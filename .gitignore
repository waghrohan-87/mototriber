# Build artefact directories are anchored with a leading slash so they only match at the
# repository root. Left unanchored, a pattern such as `plugins/` also matches `src/plugins/`
# (a standard Vue layout) and those files are dropped from the commit with no warning at all.
# The build takes its sources from git, so anything excluded here never reaches the build.
#
# node_modules/ is deliberately left unanchored: nested ones are artefacts too.

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# # Package manager lock files
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Build outputs
/dist/
/build/
/.vite/
/.turbo/

# React specific
/build
/public/hot
/public/storage
/storage/*.key

# Vue specific
/dist
/.nuxt/
.output/
.nitro/

# Next.js build output
/.next/
/out/

# Nuxt.js build / generate output
/.nuxt
/dist

# Storybook build outputs
/.out
/.storybook-out
/storybook-static/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
# ...except the backend's own .env, which is part of the project rather than a local override.
# The platform stores a project's secrets there (ProjectEnvService) and reads them back out of
# git, and it is the only thing shipped to the runtime, so a project restored from git without
# it cannot run.
!__easysite_nodejs__/.env
!__easysite_nodejs__/.env.*

# Testing
/coverage/
*.lcov
.nyc_output
/cypress/videos/
/cypress/screenshots/
/cypress/downloads/
__tests__/__snapshots__/

# IDE and editor files
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
.idea/
*.swp
*.swo
*~
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# ESLint cache
.eslintcache

# Prettier cache
.prettiercache

# StyleLint cache
.stylelintcache

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# parcel-bundler cache
.cache
.parcel-cache

# Vite cache
/.vite

# Rollup cache
.rollup.cache

# Temporary folders
/tmp/
/temp/

# Local Netlify folder
.netlify

# Vercel
.vercel

# Firebase
/.firebase/

# Sentry
.sentryclirc

# PWA
sw.*
workbox-*.js

# TypeScript
*.tsbuildinfo

# Capacitor
/.capacitor/
/android/
/ios/

# Cordova
/platforms/
/plugins/
/www/

# Electron
/dist_electron/
/release/

.env.*
!.env.example
!.env.sample
!.env.template
